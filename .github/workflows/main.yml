name: Demo-CI

on:
  push:
    branches:
      - 'main'
  pull_request:
    branches:
      - 'main'

jobs:
  Test Format code:
    runs-on: ubuntu-latest

    steps:
      - name: Hello Steps
        run: echo "Hello world"
